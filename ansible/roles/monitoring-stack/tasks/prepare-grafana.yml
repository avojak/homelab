---
- name: Create directories
  file:
    path: "{{ item }}"
    state: directory
    mode: '0775'
  with_items:
    - "{{ grafana_data_dir }}"

- name: Ensure ports open on the firewall
  ufw:
    rule: allow
    port: "{{ item.port }}"
    proto: "{{ item.proto }}"
  loop:
    - { port: "{{ grafana_port }}", proto: "tcp" }